// import Store from "../../Store";
// import { v4 as uuid } from "uuid";

export default function createGame() {
  // if (!Store.) {
  //   const game = {id: uuid(), player0: }
  //   Store.games.push();
  // }
}
